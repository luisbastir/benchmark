<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title NodeJS Benchmark
  body
    ul
      li
        a(href="/modelview/authors") Authors
      li
        a(href="/modelview/books") Books
    br
    h1 Books
    br
    table
      thead
        tr
          th ID
          th Title
          th Year
      tbody
        if books.length > 0
          each book in books
            tr
              td
                a(href="/modelview/books/" + book.id) #{book.id}
              td= book.title
              td= book.year
        else
          tr
            td(colspan=3) No items found
    br
    a(href="/modelview/books/new") Add new book
